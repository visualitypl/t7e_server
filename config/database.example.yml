default: &default
  pool: 5
  timeout: 5000
  encoding: utf8
  database: t7e_<%= Rails.env %>

development:
  <<: *default
  adapter: mysql2
  username: root
  password:
  host: localhost


# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  adapter: mysql2
  username: root
  password:
  host: localhost

production:
  <<: *default
  adapter: postgresql
  username: postgres
  password: