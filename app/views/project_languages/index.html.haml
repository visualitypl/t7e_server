%h1 Listing project_languages
%h2= "Project: #{@project.name}"
%table
  %thead
    %tr
      %th Language
      %th
      %th
      %th
      
  %tbody
    - @project_languages.each do |project_language|
      %tr
        %td= project_language.language.name
        -#%td= link_to 'Show', [@project, project_language]
        %td= link_to 'Destroy', [@project, project_language], :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Project language', new_project_project_language_path(@project)
